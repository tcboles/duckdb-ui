{
  "name": "DuckDB",
  "productName": "DuckDB",
  "type": "module",
  "build": {
    "appId": "com.tcboles.duckdb",
    "icon": "src/assets/icon.png",
    "mac": {
      "target": "dmg",
      "identity": "Thomas Boles",
      "hardenedRuntime": true,
      "gatekeeperAssess": false,
      "entitlements": "build/entitlements.mac.plist",
      "entitlementsInherit": "build/entitlements.mac.plist"
    },
    "afterSign": "build/notarize.js",
    "win": {
      "target": "nsis",
      "icon": "src/assets/icon.ico"
    },
    "linux": {
      "target": "AppImage",
      "category": "Utility"
    },
    "files": [
      "src/**",
      "build/**"
    ]
  },
  "version": "1.2.1",
  "description": "DuckDB",
  "main": "src/main.js",
  "scripts": {
    "dev": "nodemon --watch . --ext js,jsx,ts,tsx --exec \"electron .\"",
    "start": "electron .",
    "build:mac:arm64": "electron-builder --mac --arm64 --publish never",
    "build:mac:x64": "electron-builder --mac --x64 --publish never",
    "build:win": "electron-builder --win --publish never",
    "build:linux": "electron-builder --linux --publish never",
    "lint": "eslint --fix ./src",
    "publish": "git tag -d v1.2.1 && git push origin :refs/tags/v1.2.1 && git tag v1.2.1 && git push origin v1.2.1",
    "rebuild-duckdb": "electron-rebuild -f -w @duckdb/node-api"
  },
  "author": "Thomas Boles <tcboles@me.com> (https://github.com/tcboles)",
  "license": "MIT",
  "devDependencies": {
    "babel-plugin-root-import": "^6.6.0",
    "electron": "^35.1.1",
    "electron-builder": "^26.0.12",
    "electron-notarize": "^1.2.2",
    "eslint": "^9.9.0",
    "eslint-config-prettier": "^10.1.1",
    "eslint-plugin-prettier": "^5.2.1",
    "eslint-plugin-unused-imports": "^4.1.3",
    "nodemon": "^3.1.9",
    "prettier": "^3.3.3"
  },
  "dependencies": {
    "@duckdb/node-api": "^1.2.1-alpha.16"
  }
}
